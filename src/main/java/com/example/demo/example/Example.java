package com.example.demo.example;

public class Example {
	/*声明操纵查询
	@Modifying
	@Query(value = "update cart set goodsnum = ?1 where dingdanhao = ?2",nativeQuery = true)
	int updatenum(Integer goodsnum, String orderid);
	delete的自定义语句也可以这样写
	*/
}
